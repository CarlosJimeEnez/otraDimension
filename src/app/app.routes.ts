import { Routes } from '@angular/router';
import { MapHomeComponent } from './componentes/map-home/map-home.component';

export const routes: Routes = [];
routes.push({ path: '', component: MapHomeComponent });
